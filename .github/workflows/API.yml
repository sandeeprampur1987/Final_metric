name: ASP.NET CII

on:
  push:
    branches: [ master ]
jobs:

  curl:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      run: |
       #{ curl https://api.github.com/repos/miteshNeema/MavenDemo/issues | jq '.[] | {Issue Opened By: .user.login, Issue Date: .created_at, Issue Number: .id,Issue Title: .title,Issue Status: .state}'\ &&
       #curl -X GET https://api.github.com/repos/miteshNeema/MavenDemo/issues | jq -r '{Reponame: .name, Branch: .default_branch}';} >> "./output.json"
       #curl -X GET https://api.github.com/repos/mangeshpa/HelloWorldDotNet | jq -r '{Reponame: .name, Branch: .default_branch}'  >> "./output.json"
       curl https://api.github.com/repos/miteshNeema/MavenDemo/issues | jq '.[] | {Issue Opened By: .user.login, Issue Date: .created_at, Issue Number: .id,Issue Title: .title,Issue Status: .state}' >> "./output.json" 
    - name: Upload a Test Artifact
      uses: actions/upload-artifact@v2.2.4
      with:
      # Artifact name
        name: artifact
        path: ./output.json
